! function(t) {
    "use strict";

    function a() {
        i = $(document).scrollTop(), i > 0 ? $(".navbar-toggle").addClass("scrolled") : $(".navbar-toggle").removeClass("scrolled"), e()
    }

    function e() {
        if (!r) {
            var t = $(window).height() / 8;
            $(".navbar-collapse a").each(function() {
                var a = $(this).data("target");
                if (0 != $(a).length) {
                    var e = $(a).offset().top;
                    i + t > e && ($(".navbar-collapse a.active").removeClass("active"), $("a[data-target='" + a + "']").addClass("active"))
                }
            })
        }
    }

    function o() {
        var t = $(this),
            a = t.data("target");
        if (0 == $(a).length) return !0;
        $(".navbar-collapse a.active").removeClass("active"), t.addClass("active"), r = !0, $(".collapse").collapse("hide");
        var e = $(a).offset().top - ($(window).width() < 768 ? 0 : 52);
        return $("html, body").stop().animate({
            scrollTop: e
        }, 500, function() {
            r = !1
        }), !1
    }

    function n(t) {
        t.preventDefault();
        var a, e = $(window),
            o = $(".map");
        a = e.width() < 768 ? o.offset().top - e.height() + o.height() + 15 : o.offset().top - e.height() / 2 + o.height() / 2, $("html, body").stop().animate({
            scrollTop: a
        }, 500), $(".map iframe").attr("src", $(this).parents(".affiliate").data("src"))
    }

    function s(t) {
        t.preventDefault();
        var a = $(this).data("target"),
            e = $(a);
        if (0 == e.length) return !1;
        $(".navbar-nav li:nth-child(3) a").click(), "none" == e.css("display") && $("#works .row:visible").fadeOut("slow", function() {
            e.fadeIn("slow")
        })
    }
    var i, r = !1;
    $(function() {
        a(), $(window).on("scroll", a), $(".navbar-nav a").on("click", o), $(document).on("click", function(t) {
            !$(t.target).hasClass("navbar-toggle") && $(".collapse.in").length > 0 && $(".collapse").collapse("hide")
        }), $("a.location").on("click", n), $("#services a").on("click", s), $("#navigation-menu").on({
            "show.bs.collapse": function() {
                $(".navbar").addClass("grey")
            },
            "hidden.bs.collapse": function() {
                $(".navbar").removeClass("grey")
            }
        }), $("#contacts form").on("submit", function(t) {
            return t.preventDefault(), $("body").css("cursor", "wait"), $(this).find("button[type=submit]").attr("disabled", !0), $.ajax({
                url: $(this).attr("action"),
                data: $(this).serialize(),
                type: "post",
                error: function() {
                    $("body").css("cursor", "default"), $("#contacts form").find("button[type=submit]").attr("disabled", !1), alert("Возникла ошибка. Попробуйте позже.")
                },
                success: function(t) {
                    $("body").css("cursor", "default"), $("#contacts form").find("button[type=submit]").attr("disabled", !1).end().trigger("reset"), $("#contact_confirm").modal()
                }
            }), !1
        })
    })
}(jQuery);

window.onload = function(){
    var swiper = new Swiper('.swiper-container', {
        slidesPerView:3,
        navigation: {
          nextEl: '.swiper-button-next',
          prevEl: '.swiper-button-prev',
        },
        pagination: {
            el: '.swiper-pagination',
          },
      });

      var swiperCert  = new Swiper('.swiper-certification', {
        slidesPerView:3,
        spaceBetween:30,
        navigation: {
          nextEl: '.swiper-button-prev_certification',
          prevEl: '.swiper-button-next_certification',
        },

        breakpoints:{
            960:{
                slidesPerView:3,
                spaceBetween:30,
            }
        }
      });
      var swiperLisense  = new Swiper('.swiper-license', {
        slidesPerView:3,
        navigation: {
          nextEl: '.swiper-button-next_license',
          prevEl: '.swiper-button-prev_license',
        },
      });
    };

